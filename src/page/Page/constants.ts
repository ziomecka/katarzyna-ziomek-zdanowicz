import {
  aboutMeId,
  contactId,
  cvHref,
  gridId,
  knowledgeId,
  projectId,
  welcomeId,
} from '../_constants';

export default {
  welcomeId,
  aboutMeId,
  cvHref,
  gridId,
  knowledgeId,
  projectId,
  contactId,
};
